# Security Policy

## 公开范围

仓库中的后端是自建参考实现，不代表官方线上实例的配置。请不要在 Issue、PR、截图或日志中粘贴真实的：

- `API_TOKEN`、`USER_TOKEN_SECRET`、`DEEPSEEK_API_KEY`
- 数据库密码、邀请码、用户备份或导出文件
- 签名证书、私钥、服务器公网地址和部署路径

## 报告漏洞

请优先通过 GitHub Security Advisories 或私密渠道联系维护者，不要在公开 Issue 中发布可直接利用的凭据或完整 PoC。报告中请包含受影响版本、复现条件、影响范围和建议修复方式。

如果任何凭据曾经进入 Git 历史，单纯删除文件是不够的；应立即吊销并轮换该凭据，再评估是否需要清理历史。

## 自建部署最低要求

- 使用 HTTPS 和受控的反向代理。
- 为 `API_TOKEN` 与 `USER_TOKEN_SECRET` 使用独立的随机值。
- 限制管理员接口、请求体大小、请求频率和数据库账号权限。
- 将云备份视为用户敏感数据，配置加密、备份保留和删除策略。
